<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <link rel="stylesheet" href="/css/style.css"/>
    <title>Teacher Class Portal</title>
</head>
<body>
    <%- include('../partials/header') %>
    
    <main class="container">
        <h1>Teacher Portal - My Class</h1>
        <p>Welcome, **<%= teacher.name %>** (<%= teacher.role %>).</p>

        <div class="card class-info-card" style="margin-bottom: 30px;">
            <h2>Assigned Class: **<%= assignedClass %>**</h2>
            <p>Total Students: **<%= classStudents.length %>**</p>
        </div>

        <div class="card student-list-card">
            <h3>Student Roster (<%= assignedClass %>)</h3>
            
            <% if (classStudents.length > 0) { %>
                <table style="width: 100%; border-collapse: collapse;">
                    <thead>
                        <tr>
                            <th style="padding: 10px; text-align: left;">Roll Number</th>
                            <th style="padding: 10px; text-align: left;">Name</th>
                            <th style="padding: 10px; text-align: left;">Semester</th>
                            <th style="padding: 10px; text-align: left;">Current GPA</th>
                            <th style="padding: 10px; text-align: center;">Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% classStudents.forEach(student => { %>
                            <tr>
                                <td style="padding: 10px; border-top: 1px solid #ddd;"><%= student.roll %></td>
                                <td style="padding: 10px; border-top: 1px solid #ddd;"><%= student.name %></td>
                                <td style="padding: 10px; border-top: 1px solid #ddd;"><%= student.semester %></td>
                                <td style="padding: 10px; border-top: 1px solid #ddd;">
                                    <% 
                                        // Display the last recorded GPA
                                        const latestPerformance = student.performance[student.performance.length - 1];
                                    %>
                                    **<%= latestPerformance ? latestPerformance.gpa : 'N/A' %>**
                                </td>
                                <td style="padding: 10px; border-top: 1px solid #ddd; text-align: center;"><a href="/admin/search?roll=<%= student.roll %>" class="btn btn-small btn-secondary">View Profile</a></td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            <% } else { %>
                <p>No students found for this class/semester in the system.</p>
            <% } %>
        </div>
    </main>

    <%- include('../partials/footer') %>
</body>
</html>